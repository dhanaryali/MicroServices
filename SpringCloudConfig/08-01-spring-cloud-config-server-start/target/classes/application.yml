management:  endpoints:    web:      exposure:        include:        - refresh
spring:  
  cloud:
    config:
      server:        
        git:
          uri: https://github.com/dhanaryali/springcloudtraining
server:
  port: 4000
  
  
  